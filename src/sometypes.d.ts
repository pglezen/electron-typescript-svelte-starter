export interface SomeType {
  sdid: string;
  lea: string;
  sid: string;
  ack_recv?: 0 | 1;
  ack_recv_time?: string;
  ack_recv_date?: string;
  contact_person?: string;
  contact_office?: string;
  contact_phone?:  string;
}